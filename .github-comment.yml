exec:
  default:
    - when: ExitCode != 0
      template: |
        ## :x: Command Failed
        {{.JoinCommand}}
        
        Combined Output:
        {{.CombinedOutput}}
        
        Vars:
        {{.Vars.SOPS_AGE_KEY}}
        {{.Vars.TFACTION_APP_ID}}
